export enum EEventType {
  ONLINE = 'online',
  OFFLINE = 'offline',
}

export enum EEventSortBy {
  EVENT_DATE = 'date',
  CREATED_AT = 'createdAt',
}

export enum EEventFilterStatus {
  UPCOMING = 'upcoming',
  PAST = 'past',
}
